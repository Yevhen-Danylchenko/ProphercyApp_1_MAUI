<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ProphercyApp_1.ViewModel"
             x:Class="ProphercyApp_1.Views.LoginPage"
             Title="Вхід"
             BackgroundColor="#1a1a2e">
    <ContentPage.BindingContext>
        <viewmodels:LoginViewModel/>
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="30"
                             Spacing="20"
                             VerticalOptions="Center">
            <Label Text="🔮"
                   FontSize="80"
                   HorizontalOptions="Center"/>
            <Label Text="Пророцтва"
                   FontSize="36"
                   FontAttributes="Bold"
                   TextColor="#eee"
                   Margin="0,0,0,30"/>
            <Label Text="Дізнайся що тебе чекає"
                   FontSize="16"
                   TextColor="#aaa"
                   HorizontalOptions="Center"
                   Margin="0,0,0,30"/>

            <Frame BorderColor="#4a4a6a"
                   Padding="0"
                   CornerRadius="10"
                   BackgroundColor="#16213e">

                <Entry Placeholder="Електронна пошта"
                       Text="{Binding Email}"
                       Keyboard="Email"
                       TextColor="#eee"
                       PlaceholderColor="#888"
                       HeightRequest="50"
                       Margin="15,0"/>
            </Frame>

            <Frame BorderColor="#4a4a6a"
                   Padding="0"
                   CornerRadius="10"
                   BackgroundColor="#16213e">

                <Entry Placeholder="Пароль"
                       Text="{Binding Password}"
                       IsPassword="True"
                       TextColor="#eee"
                       PlaceholderColor="#888"
                       HeightRequest="50"
                       Margin="15,0"/>
            </Frame>

            <Button Text="Увійти"
                    Command="{Binding LoginCommand}"
                    BackgroundColor="#9d4edd"
                    TextColor="#fff"
                    FontSize="18"                   
                    CornerRadius="10"
                    HeightRequest="55"
                    Margin="0,20,0,0"/>
            <HorizontalStackLayout HorizontalOptions="Center">
                <Label Text="Немає акаунту? "
                       TextColor="#aaa"/>
                <Label Text="Зареєструватися"
                       TextColor="#e94560"
                       FontAttributes="Bold"
                       TextDecorations="Underline">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToRegisterCommand}"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>