<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ProphercyApp_1.ViewModel"
             x:Class="ProphercyApp_1.Views.HistoryPage"
             Title="📜 Історія"
             BackgroundColor="#1a1a2e">

    <ContentPage.BindingContext>
        <viewmodels:HistoryViewModel/>
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *">
        <!-- Заголовок -->
        <VerticalStackLayout Grid.Row="0"
                             Spacing="5"
                             Margin="0,0,0,20">
            <Label Text="Історія передбачень"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#aaa"
                   HorizontalOptions="Center"/>
            <Label Text="Тут зберігаються всі твої пророцтва"
                   FontSize="14"
                   TextColor="#aaa"
                   HorizontalOptions="Center"/>
        </VerticalStackLayout>

        <!-- Список історії -->
        <ScrollView Grid.Row="1">
            <CollectionView ItemsSource="{Binding HistoryEntries}"
                            Margin="10"
                            VerticalOptions="FillAndExpand">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="#16213e"
                               BorderColor="#4a4a6a"
                               CornerRadius="15"
                               Padding="20"
                               HasShadow="True"
                               Margin="0,0,0,15">
                            <Label Text="{Binding Text}"
                                   FontSize="16"
                                   TextColor="#eee"
                                   LineBreakMode="WordWrap"
                                   HorizontalTextAlignment="Center"/>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>
    </Grid>
</ContentPage>